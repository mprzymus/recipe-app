spring:
  datasource:
    url: jdbc:mysql://localhost:3306/prod
    username: dev_user
    password: pass
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    show-sql: false